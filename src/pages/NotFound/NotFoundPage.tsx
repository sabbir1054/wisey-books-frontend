import React from 'react';

const NotFoundPage = () => {
    return (
        <div style={{display:"flex",justifyContent:"center"}}>
            Not Found
        </div>
    );
};

export default NotFoundPage;